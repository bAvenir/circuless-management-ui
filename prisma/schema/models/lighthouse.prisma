model Lighthouse {
    id      String @id @default(cuid())
    name    String
    host    String
    pubkey  String
    privkey String
    realm   Realm

    wireguards NodeWireguard[]

    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt

    @@unique([host, realm])
}
